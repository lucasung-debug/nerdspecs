# NerdSpecs — Task Breakdown (Domain-Guarded v2.0)

> Generated by /tasks-generator from specs/screens/*.yaml + specs/domain/resources.yaml
> Date: 2026-04-13

## Overview

| Metric | Value |
|--------|-------|
| Total Phases | 6 (P0–P5) |
| Backend Resource Tasks | 10 |
| Frontend Screen Tasks | 16 |
| Verification Tasks | 2 |
| Integration Tasks | 9 |
| Total Tasks | 37 |

---

## P0: Project Setup

### P0-T0.1: Initialize Project Structure
- [ ] Create Node.js project (`package.json`, `tsconfig.json`)
- [ ] Set up TypeScript + ESLint + Prettier
- [ ] Create directory structure: `src/`, `src/commands/`, `src/resources/`, `src/generators/`, `src/storage/`, `tests/`, `templates/`
- [ ] Add dependencies: `commander`, `inquirer`, `chalk`, `ora`
- **Test**: `npm run build` succeeds without errors
- **Depends**: none

### P0-T0.2: Storage Layer Abstraction
- [ ] Define `StorageAdapter` interface with methods: `get`, `set`, `delete`, `list`
- [ ] Implement `MnemoHookAdapter` targeting mnemo-hook storage
- [ ] Implement `LocalFileAdapter` targeting `.nerdspecs/memory.json`
- [ ] Implement auto-detect logic: attempt mnemo-hook connection, fall back to local on failure
- [ ] Write shared test suite that both adapters must pass
- **Test**: both `MnemoHookAdapter` and `LocalFileAdapter` pass the identical adapter test suite
- **Depends**: P0-T0.1

### P0-T0.3: CLI Framework Setup
- [ ] Create `bin/nerdspecs` entry point using `commander`
- [ ] Register subcommands: `write`, `read`, `think`, `status`, `config`
- [ ] Add `--version` flag and `--help` text
- [ ] Render NerdSpecs ASCII header with glasses icon on startup
- [ ] Wire subcommands to stub handlers
- **Test**: `nerdspecs --help` outputs all five commands; `nerdspecs --version` returns current version
- **Depends**: P0-T0.1

---

## P1: Common Infrastructure

### P1-R1-T1: UserPreferences Resource
- [ ] Implement CRUD operations for `user_preferences` domain resource
- [ ] Fields: `language` (en/ko/both), `ask_why_built` (bool), `ask_why_need` (bool), `auto_deploy_pages` (bool), `show_progress` (bool), `timezone` (string), `created_at`, `updated_at`
- [ ] Enforce singleton pattern — only one record per user across all projects
- [ ] Apply defaults on first creation: `language=both`, all booleans `true`, `timezone=Asia/Seoul`
- **Test**: create preferences, read back, update a field, verify defaults on first run
- **Depends**: P0-T0.2
- **Parallel**: P1-R2-T1, P1-R3-T1, P1-R4-T1

### P1-R2-T1: ProjectConfig Resource
- [ ] Implement CRUD for `project_config` scoped by `repo_slug`
- [ ] Fields: `language` (en/ko/both), `auto_push` (bool), `landing_page_enabled` (bool), `landing_page_url`, `readme_sections` (object), `hook_installed` (bool), `hook_installed_at`, `created_at`, `updated_at`
- [ ] Implement `repo_slug` derivation from git remote URL in `owner--repo` format
- [ ] Apply defaults: `language=both`, `auto_push=true`, `landing_page_enabled=true`, `hook_installed=false`
- **Test**: create config, verify `repo_slug` derived correctly, update `landing_page_url`, read back
- **Depends**: P0-T0.2
- **Parallel**: P1-R1-T1, P1-R3-T1, P1-R4-T1

### P1-R3-T1: ProjectMetadata Resource
- [ ] Implement CRUD for `project_metadata` scoped by `repo_slug`
- [ ] Fields: `repo_url`, `repo_slug`, `display_name`, `detected_language`, `detected_framework`, `entry_file`, `dependency_count`, `core_files_analyzed` (string[]), `generated_summary`, `tech_stack` (object), `last_analyzed`, `nerdspecs_version`
- [ ] Implement auto-analysis: scan project directory, detect language and framework, identify entry point
- **Test**: analyze sample project directory, verify `detected_language`, `entry_file`, `dependency_count` populated
- **Depends**: P0-T0.2
- **Parallel**: P1-R1-T1, P1-R2-T1, P1-R4-T1

### P1-R4-T1: ProjectMotivation Resource
- [ ] Implement CRUD for `project_motivation` scoped by `repo_slug`
- [ ] Fields: `answer` (string), `recorded_at` (datetime), `last_used` (datetime), `language` (en/ko)
- [ ] Implement language auto-detection: Korean characters > threshold → `ko`, else `en`
- [ ] Update `last_used` timestamp on every retrieval
- **Test**: store Korean text → `language=ko`; store English → `language=en`; retrieve → `last_used` updated
- **Depends**: P0-T0.2
- **Parallel**: P1-R1-T1, P1-R2-T1, P1-R3-T1

### P1-S0-T1: CLI Common Components
- [ ] Implement `nerdspecs_header` component: ASCII logo + version
- [ ] Implement `selection_prompt` component: numbered choices, returns selected value
- [ ] Implement `free_text_input` component: `> _` prompt, rejects empty input
- [ ] Implement `progress_message` component: sequential steps with `ora` spinner
- [ ] Implement `status_row` component: `key: value [ON/OFF]` format
- [ ] Export all from `src/components/index.ts`
- **Test**: unit test each component with mocked stdout
- **Depends**: P0-T0.3
- **Parallel**: P1-R1-T1, P1-R2-T1, P1-R3-T1, P1-R4-T1

### P1-S0-T2: Onboarding Flow
- [ ] Implement 4-step onboarding: Welcome → Language selection → mnemo-hook check → Ready
- [ ] On completion, write `user_preferences` record with selected language
- [ ] First-run detection: no `user_preferences` → redirect to onboarding
- [ ] After onboarding, route user back to intended action
- **Test**: complete onboarding → preferences created; subsequent run → onboarding skipped
- **Depends**: P1-R1-T1, P1-S0-T1

---

## P2: Write Feature — Core

### P2-R5-T1: Code Analyzer Engine
- [ ] Scan project directory recursively for source code files
- [ ] Detect primary language by file extension frequency
- [ ] Detect framework by inspecting dependency manifests
- [ ] Identify entry point file (e.g., `index.ts`, `main.py`)
- [ ] Read and catalog `core_files` up to configurable limit (default: 20)
- [ ] Extract dependency list and count from `package.json`, `requirements.txt`, `go.mod`, etc.
- **Test**: analyze sample projects in Python, Node.js, Go; assert correct detection
- **Depends**: P1-R3-T1
- **Parallel**: P2-R5-T2

### P2-R5-T2: Summary Generator
- [ ] Generate one-sentence plain-language summary from code analysis
- [ ] Generate `tech_stack` structured object
- [ ] Implement LLM abstraction layer: Claude API, OpenAI API, Google Gemini API adapters
- [ ] Select active adapter from env variable or config
- [ ] Prompt engineering for non-developer-friendly output
- **Test**: generate summary for sample project; verify non-technical language (keyword blocklist)
- **Depends**: P1-R3-T1
- **Parallel**: P2-R5-T1

### P2-S1-T1: Write — Setup Check Screen
- [ ] Detect whether `project_config` exists for current repo
- [ ] First run: display detected project info + `Continue? [Y/n]` prompt
- [ ] `n` → exit gracefully; Enter → proceed
- [ ] Returning run (config exists): skip entirely
- **Test**: first-time shows setup screen; second invocation bypasses
- **Depends**: P1-R2-T1, P1-R3-T1, P1-S0-T1

### P2-S2-T1: Write — One Question Screen
- [ ] Render "Why did you build this project?" with `free_text_input`
- [ ] Language auto-detection on submitted answer
- [ ] Empty input rejection with re-prompt
- [ ] Save to `project_motivation` resource
- **Test**: Korean text → `language=ko`; empty → re-prompt; English → `language=en`
- **Depends**: P1-R4-T1, P1-S0-T1

### P2-S3-T1: Write — Memory Confirm Screen
- [ ] Retrieve stored `project_motivation`, display with relative time
- [ ] Options: 1) Yes, use it → update `last_used` 2) No → route to one-question
- [ ] No stored motivation → skip to one-question
- **Test**: option 1 updates `last_used`; option 2 routes to one-question; no data → skip
- **Depends**: P1-R4-T1, P1-S0-T1

### P2-S4-T1: Write — Progress Display Screen
- [ ] Sequential progress steps with spinner
- [ ] Steps: Reading → Analyzing → Writing README → Building landing page → Publishing
- [ ] Skip landing page steps if `landing_page_enabled=false`
- [ ] Display analyzed file list
- **Test**: all 5 steps with landing enabled; 3 steps with landing disabled
- **Depends**: P2-R5-T1, P1-S0-T1

### P2-S5-T1: Write — Success Screen
- [ ] README.md line count + bilingual label
- [ ] Landing page URL + GitHub Pages delay notice
- [ ] Hide landing section if not generated
- **Test**: correct line count; URL displayed when available; hidden when disabled
- **Depends**: P2-S4-T1

### P2-S6-T1: Write — Auto Mode (Git Hook)
- [ ] Minimal 2-line output, no prompts
- [ ] Use stored motivation silently
- [ ] No stored motivation → single-line abort + non-zero exit
- [ ] Error → single-line failure + non-zero exit
- **Test**: 2 lines exactly, exit code 0; no motivation → 1 line, exit code non-zero
- **Depends**: P2-S5-T1

### P2-S7-T1: Write Flow Orchestrator
- [ ] Route: main menu → first-time check → setup or memory-confirm → one-question → progress → success
- [ ] `--auto` flag → auto-mode directly
- [ ] Handle missing git remote gracefully
- **Test**: e2e for new project + returning project + auto-mode
- **Depends**: P2-S1-T1, P2-S2-T1, P2-S3-T1, P2-S4-T1, P2-S5-T1, P2-S6-T1

### P2-S1-V: Write Flow Verification
- [ ] E2E integration test against real sample project
- [ ] Verify: project_motivation stored, project_metadata generated, project_config created
- [ ] Verify: all screens in correct sequence
- [ ] Verify: README.md written to project root
- **Test**: e2e passes for new + returning project
- **Depends**: P2-S7-T1

---

## P3: Generation Engine

### P3-R6-T1: README Template Engine
- [ ] Markdown template: hero, plain_explanation, who_is_it_for, how_to_use, tech_stack (collapsible)
- [ ] Bilingual modes: `both`, `en`, `ko` controlled by config
- [ ] Section toggle via `readme_sections` config
- [ ] NerdSpecs footer comment
- **Test**: generate for all 3 language modes; disable tech_stack → absent; footer present
- **Depends**: P2-R5-T2, P1-R4-T1
- **Parallel**: P3-R7-T1

### P3-R6-T2: README Writer
- [ ] Write generated README.md to project root
- [ ] If `auto_push=true`: git add + commit
- [ ] Calculate and return line count
- [ ] Preserve existing non-NerdSpecs sections
- **Test**: file written, line count correct, git commit created when auto_push
- **Depends**: P3-R6-T1

### P3-R7-T1: Landing Page Template Engine
- [ ] HTML template: nav, hero, problem (3 cards), solution (before/after), how-to (3 steps), tech-footer
- [ ] Responsive: desktop 3-col → tablet 2-col → mobile 1-col
- [ ] Language toggle with localStorage persistence
- [ ] Pain points auto-generated from motivation answer via LLM
- **Test**: all 6 sections present, responsive classes, language toggle, localStorage key
- **Depends**: P2-R5-T2, P1-R4-T1
- **Parallel**: P3-R6-T1

### P3-R7-T2: Landing Page Deployer
- [ ] Write HTML to `docs/index.html` for GitHub Pages
- [ ] Check GitHub Pages configuration, output setup instructions if needed
- [ ] Update `project_config.landing_page_url` after deploy
- **Test**: file at correct path, config URL updated, URL follows `https://{owner}.github.io/{repo}/`
- **Depends**: P3-R7-T1

### P3-S8-T1: Generated README Spec Compliance
- [ ] Validate against `specs/screens/generated-readme.yaml`
- [ ] Check: hero block, plain explanation, collapsible tech details, footer comment
- [ ] Assert bilingual structure when `language=both`
- **Test**: schema validation passes for en, ko, both modes
- **Depends**: P3-R6-T2

### P3-S9-T1: Generated Landing Page Spec Compliance
- [ ] Validate against `specs/screens/generated-landing-page.yaml`
- [ ] Check: all 6 sections, responsive breakpoints, language toggle
- **Test**: schema validation passes, all sections validated
- **Depends**: P3-R7-T2

---

## P4: Read + Think Features — v0.2

### P4-R8-T1: ExplanationCache Resource
- [ ] CRUD for `explanation_cache` keyed by `owner/repo`
- [ ] Fields: `repo_url`, `owner`, `repo`, `fetched_at`, `explanation_en`, `explanation_ko`, `use_cases` (string[]), `target_audience`, `complexity_level`, `nerdspecs_version`
- [ ] 7-day TTL with auto-refresh on stale read
- **Test**: cached within 7 days → returns; expired → marked stale + refresh triggered
- **Depends**: P0-T0.2
- **Parallel**: P4-R9-T1, P4-R10-T1

### P4-R9-T1: DecisionRecord Resource
- [ ] CRUD for `decision_record` keyed by `owner/repo`
- [ ] Fields: `repo_url`, `decision` (adopt/skip/watch/undecided), `reasoning`, `reasoning_ko`, `follow_up_answer`, `recorded_at`, `reviewed_at`, `status` (active/superseded/archived)
- [ ] Vague answer detection: < 10 chars or abstract expression patterns
- [ ] Return `is_vague: boolean` flag from save
- **Test**: 5 chars → vague; "seems useful" → vague; 25-char concrete → not vague
- **Depends**: P0-T0.2
- **Parallel**: P4-R8-T1, P4-R10-T1

### P4-R10-T1: GitHub Repo Fetcher
- [ ] Extract `owner/repo` from GitHub URL
- [ ] Fetch repo metadata + README via GitHub REST API
- [ ] Decode base64 README content
- [ ] Rate limit handling (403/429)
- [ ] Validate GitHub-only URLs
- **Test**: fetch public repo, verify content; non-GitHub URL → error; 403 → rate limit message
- **Depends**: P0-T0.2
- **Parallel**: P4-R8-T1, P4-R9-T1

### P4-S10-T1: Read — URL Input + Progress + Explanation Screens
- [ ] Screen 2.0: URL input with GitHub validation
- [ ] Screen 2.1: 2-line fetch progress
- [ ] Screen 2.2: Bilingual explanation + "Do you need this?" selection
- [ ] Check `explanation_cache` before fetching
- [ ] Route: "Yes" → think-why-need; "No" → exit
- **Test**: valid URL → full flow; cached URL → skip fetch; "No" → exit cleanly
- **Depends**: P4-R8-T1, P4-R10-T1, P1-S0-T1

### P4-S11-T1: Think — Why Need + Follow-Up + Decision Saved Screens
- [ ] Screen 3.0: "Why do you need {project}?" free-text
- [ ] Check `is_vague` flag → conditionally show Screen 3.1 (follow-up)
- [ ] Screen 3.2: Decision summary display
- [ ] Decision selection: adopt/skip/watch/undecided
- **Test**: vague → follow-up appears; specific → skipped; Screen 3.2 displays all fields
- **Depends**: P4-R9-T1, P4-S10-T1

### P4-S12-T1: Status Screen
- [ ] Aggregate info from 5 resources
- [ ] Sections: Project info, Hook status, Memory stats, Landing page, Decision count
- [ ] `status_row` rendering, `—` for missing fields
- **Test**: full data → all sections; empty data → placeholders; no crash on missing resources
- **Depends**: P1-R1-T1, P1-R2-T1, P1-R3-T1, P1-R4-T1, P4-R9-T1

### P4-S13-T1: Config Screen
- [ ] Display `project_config` as key-value table
- [ ] CLI flag changes only (e.g., `--language=en`)
- [ ] Validate flag values
- **Test**: display config; change via flag → updated; invalid flag → error
- **Depends**: P1-R2-T1, P1-S0-T1

### P4-S10-V: Read + Think Flow Verification
- [ ] E2E: URL → explanation → think → decision saved
- [ ] Verify: explanation_cache stored, decision_record stored
- [ ] Verify: vague path + cache hit path
- **Test**: e2e passes for complete read+think flow
- **Depends**: P4-S10-T1, P4-S11-T1

---

## P5: Integration & Deployment

### P5-T1: Git Hook Installation
- [ ] `nerdspecs config --install-hook` command
- [ ] Write hook to `.git/hooks/post-push`
- [ ] Detect existing hooks, append rather than overwrite
- [ ] Set executable permissions cross-platform
- [ ] Update `project_config.hook_installed=true`
- **Test**: new hook → created; existing hook → appended; config updated
- **Depends**: P2-S6-T1

### P5-T2: Error Handling System
- [ ] `ERR_` code registry: `ERR_NO_MOTIVATION`, `ERR_STORAGE_UNAVAILABLE`, `ERR_GIT_NO_REMOTE`, `ERR_GITHUB_RATE_LIMIT`, `ERR_LLM_UNAVAILABLE`
- [ ] Error screen template per `specs/screens/error-generic.yaml`
- [ ] Graceful degradation: mnemo-hook failure → auto-switch to LocalFileAdapter
- [ ] Error boundaries wrapping all operations
- **Test**: each ERR_ code renders user-friendly message; mnemo-hook failure → fallback + notification
- **Depends**: P1-S0-T1

### P5-T3: Main Menu + Skill Invocation
- [ ] Screen 1.0: main menu with 4 options
- [ ] Version-locked items: read, think → "Coming in v0.2"
- [ ] Route: write → flow orchestrator; status → status screen; config → config screen
- [ ] First-run → onboarding redirect
- [ ] Claude Code skill thin wrapper
- **Test**: all options route correctly; v0.2 items show info message; first run → onboarding
- **Depends**: P2-S7-T1, P4-S12-T1, P4-S13-T1

### P5-T4: npm Package + Distribution
- [ ] Configure `package.json` for npm: `bin`, `files`, `engines` (Node 18+)
- [ ] Verify `npx nerdspecs --version` works
- [ ] Cross-platform smoke test: Windows, macOS, Linux
- [ ] Claude Code skill wrapper adapter
- [ ] Self-proving: NerdSpecs generates its own README
- **Test**: `npx nerdspecs --version` works; `--help` on all platforms; skill wrapper invokes correctly
- **Depends**: P5-T1, P5-T2, P5-T3

---

## Dependency Graph

```
P0 (Setup)
  └── P1 (Common Infrastructure)
        ├── P2 (Write Core)
        │     └── P3 (Generation Engine)
        │
        └── P4 (Read + Think, v0.2)
              └── P5 (Integration & Deployment)
```

### Parallel Execution Windows

| Phase | Parallel Group |
|-------|---------------|
| P1 | R1 \|\| R2 \|\| R3 \|\| R4 (all resources simultaneously), then S0 |
| P2 | R5-T1 \|\| R5-T2 (analyzer + summary parallel), then screens sequential |
| P3 | R6-T1 \|\| R7-T1 (README + Landing parallel), then writers sequential |
| P4 | R8-T1 \|\| R9-T1 \|\| R10-T1 (all resources parallel), then screens sequential |

### Agent Assignments

| Task Type | Agent |
|-----------|-------|
| Storage, Resource CRUD, CLI framework | backend-specialist |
| Template engines, HTML generation | frontend-specialist |
| LLM abstraction, Summary generation | backend-specialist |
| CLI component rendering | frontend-specialist |
| Integration tests, E2E verification | test-specialist |
| npm packaging, Distribution | backend-specialist |
| Screen spec compliance validation | test-specialist |
